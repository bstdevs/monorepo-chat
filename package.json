{
  "name": "root",
  "private": true,
  "scripts": {
    "start:frontend": "lerna run --scope frontend start",
    "start:backend": "lerna run --scope backend start:dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
